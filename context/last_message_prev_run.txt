TaskForge Companion is currently verified through chapter 14.

Ready chapters:
- ch00 setup
- ch01 .NET anatomy
- ch02 backend architecture
- ch03 data layer
- ch04 Projects & Issues API
- ch05 Auth JWT
- ch06 Angular foundation
- ch07 frontend architecture
- ch08 design system CSS
- ch09 shared UI kit
- ch10 routing
- ch11 HTTP and state
- ch12 projects feature
- ch13 issue board
- ch14 issue detail and comments

Latest quality pass:
- Home hero gradient fixed: the decorative glow no longer washes over the headline, and the highlighted Hebrew word uses a smoother warm gradient.
- ch14 source-tree/runtime checkpoint remains in place: tracked `client/src/index.html` data favicon, source-tree labels, mobile overflow hardening, and full model-snapshot path in content.
- ch14 content now explicitly teaches the small changed contracts that were previously only named in prose: `Issue.Comments`, comment request/response contracts, `TitleAvailabilityResponse`, and `TitleExistsAsync`.
- ch14 `issue-row.ts` comment/region now matches reality: the row imports `RouterLink` only for the title detail link, while state/optimism/store concerns stay outside the row.
- Context index now points future continuation to ch15, not older ch04/ch05 work.

Most recent gates from this pass:
- `pnpm gen:manifest` passed.
- `pnpm verify:coverage` passed: all 133 ready-chapter files are taught or toured.
- `pnpm test -- --watch=false` passed: 90 tests.
- `pnpm build` passed; the only warning is the existing Mermaid CommonJS warning.
- Direct `pnpm install` in materialized ch08 passed after the earlier transient verifier failure.
- A direct compile of every milestone in `reference/milestones.json` passed: all listed `dotnet build` and `ng build` steps from ch01 through ch14 compiled.
- Full `pnpm verify:snapshots` was attempted again but exceeded the Codex command timeout during the long reinstall/materialization flow, so use the direct milestone compile result above as the current compile evidence.
- Browser/CDP verification passed: 15 guide chapter routes checked on desktop and mobile, home gradient checked, ch14 source tree opened `CommentEndpoints.cs`, and the real ch14 app completed login, board load, issue detail, comment submit, and mobile overflow checks.

Next chapter:
- ch15 testing. Start snapshot-first. The goal is a real test pyramid for this exact app: xUnit/WebApplicationFactory for server endpoints and auth/resource checks, Vitest/TestBed for stores/forms/interceptors/routing, and a browser/E2E proof for login, board mutation, detail route, and comments.
